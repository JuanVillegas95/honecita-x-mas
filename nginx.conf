server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(glb|mp3|png|jpg|css|js)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}
